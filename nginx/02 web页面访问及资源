web页面：多个资源
    入口，资源引用
认证：
    基于IP认证
    基于用户认证
        basic
        digest
资源映射：
    Alias(路径别名)
    DocumentRoot

pv：page view(单个页面访问n次算1个PV)
uv: User View(通过独立IP、日uv、月uv)

浏览器可能支持2线程、4线程，这是对单个域名而言
    优化：多域名存放不同类型文件(图片、文本、视频)
    总线程：单域名 * 域名数

服务器接收多用户页面请求，每个页面又包含多个请求