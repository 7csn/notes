
外部访问类方法和属性：
    先从访问修饰符为 public 的找起,找到则判断 static 是否符合(不符合报错)


继承要点：
1. 方法可以被全部继承、属性除 private 外可被继承
2. final 类不被继承，final 方法不被重写
3. 非静态的方法和属性，子类对象创建时，如不重写，会复制一份己用
4. 静态的方法和属性，如不重写，子类使用时，只是借用父类的
5. self 指向定义静态属性或方法的类(或其对象)
6. static 后期静态绑定，指向实际运行时的类(或其对象)